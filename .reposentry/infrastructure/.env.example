# .env.example

```bash
# ============================================================================
# RepoSentry Environment Variables Configuration
# ============================================================================
# Copy this file to .env and fill in with your actual values.
# Never commit secrets to version control — use this file as a template only.
# ============================================================================

# ───────────────────────────────────────────────────────────────────────────
# Server Configuration
# ───────────────────────────────────────────────────────────────────────────

# Port for the RepoSentry preview/analysis server
NODE_PORT=3000

# Node environment: development, production, or test
NODE_ENV=development

# ───────────────────────────────────────────────────────────────────────────
# AI Model Configuration (GitHub Copilot CLI)
# ───────────────────────────────────────────────────────────────────────────

# AI model to use for analysis (e.g., claude-haiku-4.5, claude-opus-4.6)
# Leave empty for backend default. Available models depend on Copilot CLI installation.
# @example: claude-haiku-4.5, claude-sonnet-4, gpt-4, gpt-5
COPILOT_MODEL=claude-haiku-4.5

# Maximum retries for Copilot API calls if they fail
COPILOT_MAX_RETRIES=2

# Delay between Copilot retries in milliseconds
COPILOT_RETRY_DELAY_MS=3000

# Timeout for individual Copilot API calls in milliseconds
COPILOT_TIMEOUT_MS=180000

# ───────────────────────────────────────────────────────────────────────────
# Logging & Debugging
# ───────────────────────────────────────────────────────────────────────────

# Enable verbose/debug logging output
DEBUG=false

# Set to true to show detailed analysis progress and debug information
VERBOSE=false

# ───────────────────────────────────────────────────────────────────────────
# Analysis Configuration
# ───────────────────────────────────────────────────────────────────────────

# Output directory for analysis reports
REPOSENTRY_OUTPUT=.reposentry

# Output format: markdown, html, or json
REPOSENTRY_FORMAT=markdown

# Analysis depth: quick, standard, or deep
REPOSENTRY_DEPTH=standard

# Comma-separated glob patterns to exclude from analysis
REPOSENTRY_IGNORE=node_modules,dist,*.test.ts,build,__pycache__,.venv,vendor,target

# ───────────────────────────────────────────────────────────────────────────
# Engine Configuration
# ───────────────────────────────────────────────────────────────────────────

# Enable/disable specific analysis engines (true/false)
ENGINE_DOCS=true
ENGINE_ARCHITECTURE=true
ENGINE_SECURITY=true
ENGINE_CI=true
ENGINE_API_TESTS=true
ENGINE_PERFORMANCE=true
ENGINE_TEAM=true
ENGINE_HEALTH=true

# ───────────────────────────────────────────────────────────────────────────
# Security Configuration
# ───────────────────────────────────────────────────────────────────────────

# Security severity threshold for reports: low, medium, high, or critical
SECURITY_SEVERITY_THRESHOLD=medium

# Comma-separated patterns to exclude from security checks
SECURITY_IGNORE_PATTERNS=*.test.*,*.spec.*

# CI/CD provider to detect and analyze: github-actions, gitlab-ci, circleci, jenkins, travis
CI_PROVIDER=github-actions

# Comma-separated Node.js versions to test against in CI
CI_NODE_VERSIONS=18,20

# ───────────────────────────────────────────────────────────────────────────
# Optional: External Services (if integrated)
# ───────────────────────────────────────────────────────────────────────────

# API endpoint for custom analysis service (if applicable)
# CUSTOM_API_ENDPOINT=https://api.example.com

# Authentication token for external services
# CUSTOM_API_TOKEN=your_api_token_here

# Webhook URL for sending analysis results
# WEBHOOK_URL=https://hooks.example.com/reposentry
```